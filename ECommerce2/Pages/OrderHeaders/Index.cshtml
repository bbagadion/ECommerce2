@page
@model ECommerce2.Pages.OrderHeaders.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderHeader[0].OrderHeader_CustomerID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderHeader) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderHeader_CustomerID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OrderHeader_OrderID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OrderHeader_OrderID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OrderHeader_OrderID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
